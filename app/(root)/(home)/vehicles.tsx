import { Text } from "react-native"
import { SafeAreaView } from "react-native-safe-area-context";

const VechiclesScreen = () => {
    return (
        <SafeAreaView>
            <Text>Vehicles sxreen</Text>
        </SafeAreaView>
    )
}

export default VechiclesScreen ;