import { Text } from "react-native"
import { SafeAreaView } from "react-native-safe-area-context";

const supportScreen = () => {
    return (
        <SafeAreaView>
            <Text>support sxreen</Text>
        </SafeAreaView>
    )
}

export default supportScreen;